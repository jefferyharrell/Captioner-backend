# .env.example for Captioner-backend
# Copy to .env and fill in your real secrets (never commit .env with real tokens!)

# Dropbox OAuth 2.0 credentials (required for Dropbox backend)
DROPBOX_APP_KEY=your_app_key
DROPBOX_APP_SECRET=your_app_secret
DROPBOX_REFRESH_TOKEN=your_refresh_token

# (Deprecated) Legacy static Dropbox access token (do not use for runtime)
# DROPBOX_TOKEN=your_dropbox_token

# Optional: Dropbox root path (default: /)
# DROPBOX_ROOT_PATH=your_dropbox_folder_path  # e.g. /photos or leave blank for root

# S3 credentials (required only for S3 backend)
# S3_BUCKET=your_s3_bucket_name
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
